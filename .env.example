# Cloudflare API Configuration
# Get your API token: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone.DNS (Edit)

CF_API_TOKEN=your_api_token_here
CF_ZONE_ID=your_zone_id_here

# Optional: Email (only if using legacy Global API Key)
# CF_EMAIL=your@email.com

# Monitor Configuration (optional)
CHECK_INTERVAL=60  # seconds between health checks
AUTO_TOGGLE=true   # auto-toggle proxy based on health status
LOG_FILE=/var/log/cloudflare-dns-toggle.log
